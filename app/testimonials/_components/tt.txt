"use client";
import { Swiper, SwiperSlide } from "swiper/react";
import { Navigation } from "swiper/modules";
import "swiper/css";
import "swiper/css/navigation";
import Image from "next/image";
import { FaArrowLeft, FaArrowRight } from "react-icons/fa";

const testimonials = [
  {
    name: "Raul van Sutoyo",
    role: "UI Designer",
    rating: 5,
    text: "Adding live social proof was the #1 driver of increased revenue in all my experiments while at Airkey. And the brand must survive atleast 1 year.",
    avatar: "https://i.ibb.co.com/Kzpy8v0S/download.jpg",
  },
  {
    name: "Jane Doe",
    role: "Product Manager",
    rating: 4,
    text: "This product changed how I manage my daily workflow. Highly recommended!",
    avatar: "https://i.ibb.co.com/Kzpy8v0S/download.jpg",
  },
];

export default function TestimonialList() {
  return (
    <div className="">
      {/* Right Side Testimonial Slider */}
      <div className="w-full">
        <h2 className="text-3xl font-bold mb-6">What Peoples say</h2>

        <Swiper
          modules={[Navigation]}
          navigation={{
            nextEl: ".custom-next",
            prevEl: ".custom-prev",
          }}
          spaceBetween={30}
          slidesPerView={1}
          className="pb-12"
        >
          {testimonials?.map((testimonial, idx) => (
            <SwiperSlide key={idx}>
              <div className="flex flex-col lg:flex-row items-center justify-center gap-12 px-6 py-12">
                {/* Left Side Image */}
                <div className="relative w-full lg:w-1/2 flex justify-center">
                  {/* White card behind */}
                  <div className="absolute left-44 -bottom-8 w-72 h-72 bg-cyan-400 rounded-lg shadow-lg"></div>

                  {/* Main image */}
                  <Image
                    width={300}
                    height={300}
                    src="https://i.ibb.co.com/Kzpy8v0S/download.jpg"
                    alt="testimonial"
                    className="relative w-72 h-72 object-cover rounded-lg z-30 shadow-xl"
                  />

                  {/* Decorative arrow */}
                  <div className="absolute top-1 -right-2 rotate-20">
                    <Image
                      width={300}
                      height={300}
                      src="https://i.ibb.co.com/v4gqzhkt/Vector-3-Picsart-Background-Remover.png"
                      alt="testimonial"
                      className="w-full h-full"
                    />
                  </div>
                </div>

                <div className="w-full lg:w-1/2">
                  <p className="text-gray-600 mb-6 italic text-lg leading-relaxed">
                    "{testimonial.text}"
                  </p>

                  <div className="flex items-center gap-4">
                    <Image
                      width={50}
                      height={50}
                      src={testimonial.avatar}
                      alt={testimonial.name}
                      className="w-12 h-12 rounded-full object-cover"
                    />
                    <div>
                      <p className="font-semibold">{testimonial.name}</p>
                      <p className="text-sm text-gray-500">{testimonial.role}</p>
                      <div className="flex text-yellow-400">
                        {Array.from({ length: testimonial.rating }).map((_, i) => (
                          <span key={i}>★</span>
                        ))}
                      </div>
                    </div>
                  </div>
                </div>
              </div>
            </SwiperSlide>
          ))}
        </Swiper>

        {/* Custom Navigation */}
        <div className="flex justify-end gap-4 bottom-10">
          <button className="custom-prev w-12 h-12 rounded-full bg-white shadow-md flex items-center justify-center text-gray-600 hover:bg-gray-100">
            <FaArrowLeft />
          </button>
          <button className="custom-next w-12 h-12 rounded-full bg-cyan-400 shadow-md flex items-center justify-center text-white hover:bg-cyan-500">
            <FaArrowRight />
          </button>
        </div>
      </div>
    </div>
  );
}